# Архитектурные улучшения — бэклог issues

*Составлено 2026-07-14 по итогам разбора AI-Office на 8 уровней архитектуры
(Business/Product/Domain/System/Software/Technical/Infrastructure/Data) —
живым кодом, не по старым аудитам. Формат — как GitHub issues (нет `gh` CLI
в этом окружении, поэтому список здесь; при появлении доступа к GitHub
каждый пункт переносится один в один).*

Каждый пункт: приоритет, эффект от изменения, затронутые файлы, статус.
Статус `open` — не начато. Закрывая issue, меняй статус на `done` с датой,
не удаляй пункт (история решений — тоже часть документации).

---

## Critical

### #1 — CI-workflow на `tests/run_all.py`
**Эффект:** регрессии ловятся до мержа, а не в следующей сессии живого прогона. 11+ реальных тест-файлов сейчас простаивают между ручными запусками.
**Файлы:** новый `.github/workflows/tests.yml` (или аналог, если CI не GitHub Actions).
**Статус:** done (2026-07-14) — `.github/workflows/tests.yml` + `RUN_ALL_SKIP` в `tests/run_all.py`

### #2 — Единый тип `Artifact`
**Эффект:** новый тип "что команда произвела" (после лидов/сайтов) регистрируется, а не требует руками добавлять его в 3 места — тот же баг, что чинили для `results.py` в этой сессии, но для более широкого класса сущностей (боты, деплои, документы).
**Файлы:** новый `src/office/artifact.py` (адаптер, не переписывание `sites.py`/`tenant_apps.py`).
**Статус:** done (2026-07-14) — `artifact.py` + регистрация в `sites.py`/`tenant_apps.py` + `world.py.artifacts_count`

### #3 — Формализовать конвенцию слоёв в CLAUDE.md
**Эффект:** явное правило «домен только в `office/*`, HTTP только в `server.py`, персистентность только через `context.*`» — вместо молчаливого соглашения, которое легко нарушить в спешке (тихий баг: гонка записи, потеря атомарности).
**Файлы:** `CLAUDE.md` §4 (Инварианты).
**Статус:** done (2026-07-14) — новый инвариант «Три слоя, три места — не смешивать»

---

## High

### #4 — Единый DNA-класс вместо `philosophy.py` + `constitution.py`
**Эффект:** сейчас `world.py` читает оба файла «как DNA-часть снапшота» без версионирования — риск того же рассинхрона, что уже был между Milestones и Plan.
**Файлы:** `src/office/philosophy.py`, `src/office/constitution.py`, `src/office/world.py`.
**Статус:** done (2026-07-14) — новый `src/office/dna.py` (`snapshot()`+`version()` как хеш содержимого); `world.py.identity` теперь один вызов `dna.snapshot()`. Хранение (`philosophy.json`/`constitution.json`) и их API/потребители (`server.py`, фронт `ProfileTab`, `loop.py.requires_ok`) сознательно НЕ тронуты — миграция хранения без функциональной причины была бы риском ради косметики, см. докстринг `dna.py`.

### #5 — Billing-provider абстракция поверх apinet
**Эффект:** смена/добавление LLM-провайдера — конфиг, а не правка кода в нескольких местах. Сейчас только обработка «403 quota» внутри `execution.py`.
**Файлы:** `src/office/execution.py`, `core/llm.py`.
**Статус:** done (2026-07-14) — новый `src/core/billing_provider.py` (реестр `BillingProvider`, `apinet` зарегистрирован как первый); `core/llm.py.is_quota_error/is_model_unavailable_error` делегируют активному провайдеру, сигнатура и поведение не изменились (`execution.py` не тронут). Изначально положил модуль в `office/` — заметил, что это развернуло бы направление зависимости `core→office` (core/llm.py ничего из office/ не импортировал), перенёс в `src/core/`, где ему место по слоям из issue #3.

### #6 — Расширить `ui_prefs.py`-персонализацию за пределы «Результатов»
**Эффект:** паттерн уже готов и проверен живьём (порядок/видимость подвкладок) — копирование дёшево; несогласованность («тут можно скрыть вкладку, а тут нет») — реальный UX-баг.
**Файлы:** `src/office/ui_prefs.py`, `webapp/src/app/views/ResourcesView.tsx` (4 подвкладки уже сейчас).
**Статус:** done (2026-07-14) — `ResourcesView.tsx` получил тот же `orderTabs`/gear-панель/`api.uiPrefs("resources")`, что уже был в `ResultsView.tsx`. Бэкенд подтверждён живым тестом (`GET/POST /api/ui-prefs/resources` — сохранение/чтение корректны). Визуальный клик-тест в браузере заблокирован независимым багом (см. #12) — не удалось довести до конца в этой сессии, полагаюсь на `tsc`+`build` чистые + код идентичен уже проверенному паттерну.

---

## Medium

### #7 — Очередь задач / воркер-пул вместо одного `asyncio.Task` на тенанта
**Эффект:** снимает потолок масштабирования одного event loop. Триггер к запуску — измеренная просадка при росте числа одновременно активных офисов, не «на всякий случай».
**Файлы:** `src/office/loop.py`.
**Статус:** open (ждёт триггера)

### #8 — Dockerfile приложения + reverse-proxy + TLS
**Эффект:** путь к проду за пределами одного хоста. Раньше блокирующий пункт — в репозитории не было ни одной строчки деплой-инфраструктуры (только `docker/sandbox.Dockerfile` для песочницы исполнения кода агентов, не для самого приложения).
**Реализовано:**
- `Dockerfile` — двухстейджевая сборка: Node собирает `webapp/` (`vite build` → `static/webapp`, как в CLAUDE.md §2), затем Python 3.13-slim рантайм копирует ТОЛЬКО результат сборки — в финальном образе нет Node/node_modules/исходников фронта. Непривилегированный пользователь (тот же приём, что уже в `docker/sandbox.Dockerfile`). Без `--reload` — та логика в `scripts/run.py` существует только для локальной разработки.
- `.dockerignore` — `data/` (per-tenant состояние, НИКОГДА не в слой образа — утечка данных клиентов в registry), `.env`, node_modules/dist, кеши.
- `docker-compose.yml` — сервис `app` (volume `app-data:/app/data` — единственное состояние, обязанное пережить пересборку) + сервис `proxy` (Caddy — автоматический TLS через Let's Encrypt без отдельного certbot-контейнера, самообновляющийся сертификат).
- `Caddyfile` — reverse-proxy на `app:8000`, `flush_interval -1` (без буферизации — `/events` в `server.py` держит долгоживущий SSE-стрим, буферизация проксёра держала бы чанки). `DOMAIN=localhost` по умолчанию → self-signed сертификат для локальной проверки без реального домена.
- `README.md` — раздел «Прод-деплой» с командами запуска.
**Осознанно НЕ сделано:** отдельный CI-шаг сборки/паблиша образа (issue #1 уже покрывает `tests/run_all.py`, публикация образа в registry — отдельная задача под конкретный registry/реестр, не начинать без выбора площадки хостинга); nginx вместо Caddy (Caddy закрывает TLS без ручного certbot/cron — меньше движущихся частей для этого масштаба, тот же принцип "не JSON→Postgres без триггера" из issue #10).
**Файлы:** `Dockerfile`, `.dockerignore`, `docker-compose.yml`, `Caddyfile`, `README.md`.
**Проверка:** Docker не установлен в этой рабочей среде (тот же ограничивающий фактор, что и у `docker/sandbox.Dockerfile` и у issue "Docker sandbox по умолчанию" из более раннего аудита) — `docker build`/`docker compose up` вживую НЕ прогонялись. Проверено то, что возможно без Docker: `docker-compose.yml` — валидный YAML (`yaml.safe_load`), пути в `Dockerfile` сверены с `webapp/vite.config.ts` (`outDir: "../static/webapp"`) и фактическим `webapp/package-lock.json`, `Caddyfile`-синтаксис исправлен после обнаружения реальной ошибки при само-ревью (смешение безусловного `reverse_proxy` с `handle @sse` — недопустимая комбинация роутинга в Caddy, заменено на глобальный `flush_interval -1`).
**Статус:** done (2026-07-15) — с честной оговоркой: не запущено вживую из-за отсутствия Docker в среде разработки; первый реальный `docker compose up` на хосте с Docker — необходимая проверка перед боевым использованием.

### #9 — Формализовать CQRS на World Model
**Эффект:** не новая архитектура, а имя уже существующему паттерну (`world.py` уже неявно разделяет запись через модули-источники и чтение через `snapshot()`/`context_block()`) — снижает путаницу для следующего, кто будет читать этот модуль.
**Реализовано:**
- `src/office/world.py` — докстринг модуля назвал паттерн явно: Query-сторона — ИСКЛЮЧИТЕЛЬНО этот файл (`snapshot`/`diff`/`context_block`/`snapshot_by_id`/`snapshots_between`), Command-сторона — каждый модуль-источник сам (`brief.set_brief`, `objectives.add/update`, `plan.set_tasks` и т.д.), команды живут рядом с данными, не в `world.py`. Явный практический вывод дописан прямо в докстринг: менять мир — правь модуль-источник, читать что-то новое — новое поле в `snapshot()`, не отдельный ad-hoc запрос в обход агрегатора.
- `CLAUDE.md` (таблица §3.7) — строка `office/world.py` получила короткую пометку «CQRS: Query — только этот файл, Command — каждый модуль-источник сам».
**Осознанно НЕ сделано:** переименование функций/файлов под CQRS-терминологию (`snapshot()`→`query()` и т.п.) — это чисто документационная задача (имя уже существующему), а не рефакторинг кода; переименование публичного API без функциональной причины было бы риском ради косметики (тот же принцип, что и в issue #4 про `dna.py`).
**Файлы:** `src/office/world.py`, `CLAUDE.md`.
**Статус:** done (2026-07-15) — только документация/докстринг, код `world.py` не менялся, `py_compile` чист.

---

## Low — осознанно не делать сейчас

### #10 — Postgres вместо JSON для домена тенанта
**Почему не сейчас:** JSON дёшев, human-readable, на нём уже держится Sandbox Company (клонирование тенанта копированием папки). Миграция раньше реальной причины (объём/конкурентная запись/полнотекстовый поиск) — чистая потеря. Задел уже есть в докстринге `saas/db.py` («Postgres появится в проде, Phase 6»).
**Статус:** backlog, не начинать без триггера

### #11 — Redis / кеш-слой вне процесса
**Почему не сейчас:** нет измеренного бутылочного горлышка — in-process кеш пока справляется.
**Статус:** backlog, не начинать без триггера

---

## Найдено по ходу (не из исходного разбора)

### #12 — Digest/Understanding-попапы блокируют клики по ВСЕЙ странице после закрытия
**Баг:** попап «Пока тебя не было» (Morning Digest) — после `location.reload()` появляется поверх контента; клик по «×» скрывает видимый контент попапа, но НАВИГАЦИЯ (NavRail, любые кнопки) остаётся полностью нерабочей — клики физически не доходят до React-обработчиков нигде на странице. Живой репро в этой сессии: подтверждён `elementFromPoint`/ручным `dispatchEvent` — клик доходит до DOM-узла, но не до `onClick`; воспроизводится стабильно после `reload()`, лечится только повторной SPA-навигацией из другого источника (deep-link), не самим закрытием попапа. Похоже на тот же класс проблемы, что уже упоминался в `docs/handoff.md` («обе всплывашки — абсолютно спозиционированные поверх ВСЕГО контента... оставались»), но с более тяжёлым следствием — не просто визуальное наложение, а полная потеря интерактивности приложения до ручного `location.reload()` без reload-триггера или смены вкладки.
**Эффект от фикса:** сейчас это блокирующий баг всей навигации, не только эстетика — пользователь, дождавшийся дайджеста, может потерять способность кликать куда-либо в приложении.
**Файлы:** вероятно `App.tsx` (компонент попапа Digest, оверлей-контейнер — див с `position: fixed`/`absolute` inset:0, который остаётся в DOM или сохраняет `pointer-events` после закрытия).
**Статус:** done (2026-07-14) — `App.tsx`: `pointerEvents` на digest/understanding-попапах теперь явно завязан на `digestOpen`/`understandingOpen`, а не на факт присутствия узла в DOM. Корневая причина (почему AnimatePresence иногда не завершает exit) не изолирована на 100% (вероятный кандидат — `setDigestOpen(false)` в эффекте `[view]` прилетает в том же тике, что смена вкладки, см. комментарий в коде), но фикс не зависит от причины: даже если «призрачный» узел останется в DOM невидимым, он больше не сможет перехватывать клики. Живая проверка — точный репро (открыть дайджест → закрыть × → кликнуть NavRail) на реальном тенанте: навигация в «Ресурсы» сработала, `/api/ui-prefs/resources` вызван корректно.

### #13 — Роль Designer возвращена как отдельная от Developer (не повтор старой ошибки)
**Запрос:** владелец захотел полноценного дизайнера — референсы, бренд-бук, Figma, утверждение направления с владельцем ДО того, как разработчик начнёт строить; плюс короткий сценарий «просто показать концепт в Telegram-канале».
**Напряжение, которое решали:** `designer` и `developer` уже БЫЛИ двумя ролями на ОДИН артефакт (`site/`) — слиты в одну роль из-за реального прод-бага (второй систематически переписывал то, что собрал первый). Возвращать `designer` вслепую значило воспроизвести тот же баг. Решение: `designer` владеет ДРУГИМ артефактом (`docs/brand_book.md`, тип `"doc"`) — граница артефактов, а не дублирование одного и того же; `developer` остаётся единственным, кто пишет в `site/`.
**Реализовано:**
- `src/office/roles.py` — `ROLE_META["designer"]` (department `tech`, artifact-граница явно прокомментирована).
- `src/office/builtin_roles/designer.md` — идентичность роли, жёсткая граница («никогда write_file в site/»).
- `src/office/creative_brief.py` — интейк вкуса владельца (тон/референс/что избегать), один раз на тенанта, через `ask_user`.
- `src/office/builtin_skills/brand_book.md` — designer'ский скилл: интейк → референсы (`web_search`) → каталог из 12 направлений (перенесён из `landing_conversion.md`, где раньше решение единолично принимал marketer без утверждения владельцем) → `docs/brand_book.md` + строка «Стиль: …» в `docs/site_content.md` → ОБЯЗАТЕЛЬНОЕ явное «да» через `ask_user` перед тем, как считать задачу готовой.
- `src/office/builtin_skills/figma_concept_to_telegram.md` — короткий сценарий (без Project/Task): `figma.export_images` → `telegram.send_photo`, для случая «просто покажи макет в канале».
- `src/office/plan.py` (`_derive_artifacts`), `src/office/prompt_builder.py` (стек-подсказка сборки сайта — только `developer`, не `designer`) — техническое обеспечение границы.
- Почищено 4 старых скилла (`analytics_counter`/`qa_site_selfcheck`/`responsive_mobile_first`/`web_accessibility`), где `designer` оставался как алиас с прошлого слияния ролей — все про построенный сайт, не про бренд-бук.
- Фронтенд: `webapp/src/data/roles.ts` (описание/скиллы), `TeamView.tsx` (`ROLE_RU.designer` → «Дизайнер», было «Разработчик»).
- `src/agents/leaders.py` (`_ROLE_DESC`) — описание роли для промпта CTO при найме.
**Не в этой итерации (см. #7/#8 в исходном разборе Now/Next/Later):** Figma WRITE (создание мокапов) — нужен другой OAuth scope, сейчас только `file_content:read`; формальная сущность «Sitemap» для многостраничных сайтов; ветвление сценариев на уровне Planning Engine.
**Известное ограничение (не техническое исполнение, а прод-инвариант без кода):** ничто технически не мешает designer вызвать `write_file` на `site/*` — граница держится на тексте роли/скилла, как и у остальных non-coding ролей (marketer/analyst) сегодня; это тот же уровень строгости, что уже принят в проекте. **Закрыто в #15** (code-level enforcement).
**Файлы:** см. список выше.
**Статус:** done (2026-07-14) — 4 теста (`test_design_skills.py` ×3, `test_run_quality_fixes.py` ×2, `test_design_style.py` ×1), кодировавшие старое поведение «designer = алиас developer», обновлены под новую границу; `tests/run_all.py` — 50/50 чисто.

### #14 — Figma-write и Sitemap (следующий шаг после #13)
**Запрос:** реализовать Figma-write (designer создаёт мокапы в Figma) и формальную сущность Sitemap для многостраничных сайтов.
**Важное открытие — Figma-write физически невозможен через нашу интеграцию:** публичный REST API Figma (`api.figma.com/v1/*`) не имеет и никогда не имел эндпоинтов создания дизайн-контента (фрейм/шейп/текст) — только чтение + комментарии/webhooks/variables (Enterprise). Инструменты `mcp__figma-bridge__create_frame` и т.п., доступные в этой сессии — это ДРУГОЙ механизм (плагин внутри открытой Figma конкретного пользователя), не облачный API, который можно вызвать за произвольного тенанта через его OAuth-токен. Строить фейковый "Figma-write" через `use_integration` было бы нечестно — не делали.
**Решение (по указанию владельца "думай широко" — Figma нужна не только бренд-буку):** усилена READ-сторона как переиспользуемая способность:
  - `builtin_skills/brand_book.md` — новый шаг 0.5: если у клиента уже есть готовый Figma-файл (ссылка в референсе/брифе), designer читает его структуру (`figma.get_file` — только текст: названия страниц/фреймов, БЕЗ картинки, т.к. `core/llm.py` не поддерживает vision) как ПРИОРИТЕТНЫЙ сигнал направления, вместо гадания по каталогу.
  - `builtin_skills/vite_react_site.md` — developer тоже может прочитать Figma-файл клиента (если он упомянут в бренд-буке) за структурными подсказками (сколько экранов, порядок) — не заменяет «Стиль: …», а уточняет структуру.
**Sitemap — реализовано полностью:**
  - `src/office/sitemap.py` — read-side helper (по образцу `creative_brief.py` после исправления, см. ниже): читает `docs/sitemap.md`, который designer сам пишет/читает через `write_file`/`read_file`; `is_approved()` смотрит на маркер-строку «Статус: одобрено владельцем», которую designer дописывает ТОЛЬКО после явного «да».
  - `builtin_skills/sitemap_planning.md` — designer'ский скилл: 3-6 страниц (не больше — разрастание карты без причины усложняет и разработку, и навигацию), явное одобрение владельцем.
  - `builtin_skills/vite_react_site.md` — developer читает `docs/sitemap.md`; если есть и одобрена — строит `react-router-dom` по карте; если нет или черновик — обычный лендинг (не начинает многостраничную структуру раньше времени).
  - `prompt_builder.py` — новый слот `sitemap_section` для designer/developer.
**Побочная находка — исправлен реальный баг в `creative_brief.py` (issue #13):** модуль был написан с `capture()`/`load()`/`is_set()` как отдельным JSON-хранилищем (`ctx.read_json`), которое ДОЛЖЕН был вызывать агент напрямую — но агенты вызывают ТОЛЬКО зарегистрированные инструменты (`write_file`/`read_file`/`ask_user`), не произвольные Python-функции `office/*.py`. Это значило, что `capture()` НИКОГДА не вызывался — интейк вкуса молча не работал с момента коммита issue #13, несмотря на чистые тесты (тестов на реальный вызов из агента не было). Исправлено на файловый паттерн: `creative_brief.py` теперь read-only helper над `docs/creative_brief.md`, который designer пишет через `write_file` — тот же принцип, что уже проверен в `design_style.ensure_style_line`. `sitemap.py` сразу спроектирован правильно, по этому же паттерну.
**Файлы:** `src/office/sitemap.py` (новый), `src/office/creative_brief.py` (переписан), `src/office/builtin_skills/{brand_book,vite_react_site,sitemap_planning}.md`, `src/office/builtin_roles/designer.md`, `src/office/roles.py`, `src/office/prompt_builder.py`.
**Статус:** done (2026-07-14) — живой смоук-тест (не полный прогон офиса — не было причины тратить реальные LLM-токены на текстовую логику): `sitemap.is_set/is_approved/prompt_block`, `creative_brief.is_set/prompt_block`, полная сборка `prompt_builder.task_context` для designer/developer/marketer — подтверждена граница (designer НЕ получает стек-подсказку сборки сайта, developer получает; sitemap/creative_brief блоки появляются только у нужных ролей). `tests/run_all.py` — 50/50 чисто.

### #15 — Enforcement границ ролей на уровне кода (закрывает «известное ограничение» из #13)
**Запрос:** #13 честно документировал, что граница `designer` никогда не пишет в `site/*` держится ТОЛЬКО текстом роли/скилла — технически ничто не мешало LLM вызвать `write_file("site/index.html", ...)`. Нужен настоящий code-level enforcement, а не только промпт.
**Почему это не гипотетический риск:** `ROLE_META[role]["tools"]` — чисто описательные метаданные, `agent_factory.py` выдаёт КАЖДОМУ агенту полный набор файловых тулов независимо от роли (никакой фильтрации по роли не было и нет). Единственная реальная преграда — надежда, что модель прочитает и не нарушит текст `builtin_roles/designer.md`.
**Реализовано:**
- `src/office/roles.py` — новый словарь `ROLE_DENY_PREFIXES` (сейчас: `designer` → `("site/",)`) и функция `path_denied(role, path) -> str` (пусто — разрешено; иначе — текст отказа для tool-результата). Единственный источник правды для enforcement, отдельно от свободного текста в `constraints`.
- `src/agents/file_tool_handlers.py` — `_handle_write_file`/`_handle_delete_file` вызывают `roles.path_denied(role, path)` СРАЗУ после нормализации пути (это единственный фрейм, где одновременно доступны и `role` из замыкания `build(agent_id, role, ...)`, и целевой `path`) и возвращают отказ ДО вызова `workspace_module.write_file`/`delete_file` — designer физически не может записать/удалить файл в `site/*`, а не «не должен по инструкции».
**Осознанно НЕ сделано:** allowlist (разрешить только явно перечисленные префиксы) вместо denylist — оставили denylist, т.к. у большинства ролей (developer/marketer/analyst/integrator) сегодня нет задокументированного архитектурного ограничения «пишет только сюда», и придумывать его сейчас значило бы вводить границу, которой нет в реальном инварианте проекта, только ради симметрии. Расширять `ROLE_DENY_PREFIXES` — только под новый явно задокументированный инвариант (по образцу designer), не «на всякий случай».
**Тесты:** `tests/test_agent_tool_handlers.py` — 4 новых теста (`test_designer_write_to_site_is_denied`, `test_designer_write_to_docs_is_allowed`, `test_developer_write_to_site_is_allowed`, `test_designer_delete_in_site_is_denied`).
**Файлы:** `src/office/roles.py`, `src/agents/file_tool_handlers.py`, `tests/test_agent_tool_handlers.py`.
**Статус:** done (2026-07-15) — `tests/run_all.py` — 50/50 чисто (34/34 в `test_agent_tool_handlers.py`, включая 4 новых).

### #16 — Vision: designer/developer реально «видят» изображение (закрывает пробел из #14)
**Запрос:** следующий пункт бэклога после #15 — `core/llm.py` не поддерживал vision вообще (grep на `image_url`/`vision`/`base64` — ноль совпадений), хотя в `.env`/каталоге моделей уже есть вижн-модели (`qwen3-vl-plus`, `qwen3-vl-flash`). Из-за этого designer читал экспорт Figma (`figma.export_images`) только как ссылку на картинку, которую физически не мог интерпретировать — шаг 0.5 в `brand_book.md` прямо констатировал «БЕЗ картинки, у тебя нет инструмента посмотреть».
**Реализовано:**
- `src/office/models.py` — `vision_model()`: `VISION_MODEL` из `.env` или самая дешёвая модель из `PRESETS` с меткой `vision` в описании (сейчас `qwen3-vl-flash`). Отдельно от `for_agent`/`get_default` — текстовая модель тенанта (часто `glm-4.5-flash`) картинку не примет.
- `src/core/llm.py` — новая функция `describe_image(image_url, question, agent_id, model=None)`: ОДНОРАЗОВЫЙ (не агентный, без tool-loop) вызов chat.completions с multimodal-контентом (`{"type": "image_url", ...}`) — сознательно отдельно от `run_agent`, а не веткой внутри него (vision — редкий разовый запрос, не полноценный агентный цикл; смешивание форматов messages внутри `_mask_old_observations`/истории усложнило бы без пользы). Инкрементальный учёт расхода (`costs.record`) — тот же паттерн, что в `run_agent`.
- `src/agents/tool_schemas.py` — `ANALYZE_IMAGE_TOOL`; `src/agents/vision_tool_handlers.py` (новый, по образцу `file_tool_handlers.py`) — `_handle_analyze_image` зовёт `llm.describe_image`.
- `src/agents/agent_factory.py` — тул и хендлер подключены ТОЛЬКО ролям `designer`/`developer` (`_VISION_ROLES`) — остальным ролям инструмент не нужен и не предлагается, каталог тулов не раздувается.
- `src/office/roles.py` — `analyze_image` добавлен в `tools` designer/developer (метаданные для UI/промпта).
- `src/office/builtin_skills/brand_book.md` — шаг 0.5 переписан: после `figma.export_images` designer теперь реально вызывает `analyze_image` на полученную ссылку и получает текстовое описание палитры/типографики/композиции — не только имена фреймов.
**Файлы:** `src/office/models.py`, `src/core/llm.py`, `src/agents/tool_schemas.py`, `src/agents/vision_tool_handlers.py` (новый), `src/agents/agent_factory.py`, `src/office/roles.py`, `src/office/builtin_skills/brand_book.md`.
**Тесты:** `tests/test_agent_tool_handlers.py` — 3 новых (`test_analyze_image_available_to_designer`, `test_analyze_image_not_available_to_marketer`, `test_analyze_image_calls_describe_image_and_returns_result`, последний мокает `llm.describe_image` — реальный вижн-вызов деньги стоит, не гоняли).
**Статус:** done (2026-07-15) — `tests/run_all.py` — 50/50 чисто, `tsc --noEmit` чист (изменений во фронте не потребовалось — `webapp/src/data/roles.ts` уже описывал designer через «Figma»/«референсы», что теперь буквально верно).

### #17 — Бренд-бук: выбор из 2-3 вариантов вместо одного да/нет (n8n-качество ветвления)
**Запрос:** исходный запрос владельца на designer явно просил ветвление сценариев «как в n8n и даже лучше». До этого пункта `brand_book.md` уже давал designer возможность предлагать направление и получать одобрение, но ТОЛЬКО одно направление — владелец мог либо согласиться, либо попросить другое с нуля; реального выбора («вариант A или B») не было ни на одном шаге.
**Реализовано (`src/office/builtin_skills/brand_book.md`, только текст скилла — без изменений кода):**
- Шаг 2 переписан: designer подбирает 2-3 РАЗНЫХ по характеру кандидата из каталога (не два похожих оттенка одного и того же — контраст должен быть настоящим), кроме явного случая «есть готовый Figma-макет клиента, диктующий направление» или «владелец явно попросил не показывать варианты» — короткий путь остаётся доступным, не церемония ради церемонии.
- Шаг 3: пока владелец не выбрал — `docs/brand_book.md` содержит ЧЕРНОВИК с несколькими кандидатами; `docs/site_content.md` (строка «Стиль: …», которую читает developer) сознательно НЕ пишется на этом шаге — иначе developer мог бы начать по незавершённому выбору.
- Шаг 4 переписан на реальную ветвление-логику с тремя исходами одного `ask_user`: (а) владелец называет вариант → финал; (б) просит точечную правку одного варианта → корректировка + отдельное повторное подтверждение именно этой версии; (в) «ни один» → возврат к шагу 2 за новыми кандидатами, не повторяя отклонённые. Только после однозначного выбора — `docs/site_content.md` + `docs/brand_book.md` перезаписываются финальной версией (не дописываются к черновику).
**Изначально НЕ планировалось (см. #18 ниже):** UI-виджет для визуального сравнения вариантов — на момент закрытия #17 казалось лишней церемонией без явного запроса; следующим же шагом владелец попросил продолжить по списку, и это стало #18.
**Файлы:** `src/office/builtin_skills/brand_book.md`.
**Тесты:** без изменений кода — `tests/test_design_style.py`/`test_design_skills.py` (проверяют каталог направлений и роутинг скилла, не задеты) прогнаны заново.
**Статус:** done (2026-07-15) — `tests/run_all.py` — 50/50 чисто (первый прогон после правки показал 3 упавших файла `test_execution.py`/`test_milestone_task_link.py`/`test_planning_engine.py`, все три зелёные поодиночке и на повторном полном прогоне — известный класс флакейности межтестовой изоляции тенантов, не регрессия от этой правки, которая вообще не трогает Python-код).

### #18 — Цветные свотчи для выбора направления в чате (закрывает «осознанно не сделано» из #17)
**Запрос:** следующий пункт после #17 — реальный UI для сравнения кандидатов, а не только текст в `ask_user`.
**Реализовано:**
- `src/office/builtin_skills/brand_book.md` (шаг 4) — формат вопроса зафиксирован МАШИНОЧИТАЕМО: каждый кандидат отдельной строкой `<Буква>) <Название> (<accent hex> / <bg hex>) — <почему>`, hex дословно из каталога направлений (accent первым). Раньше формат был вольным пересказом — теперь это контракт между designer и фронтом.
- `webapp/src/app/views/ChatsView.tsx` — `parseDesignCandidates()` (регэксп по этому формату) + `DesignCandidatePicker` — рисует под сообщением-вопросом ряд кликабельных карточек с двухцветным свотчем (bg+accent), названием и обоснованием; клик подставляет `"<Буква>) <Название>"` в поле ввода (не отправляет автоматически — владелец может дополнить/поправить перед отправкой, тот же принцип, что и у обычного текстового ответа).
**Осознанно НЕ сделано:** авто-отправка по клику — один клик подставляет текст, отправка остаётся явным действием владельца (Enter/▶), чтобы не потерять возможность уточнить ответ («Вариант A, но без коричневого»), которую явно предусматривает шаг 4 скилла.
**Первый прецедент в системе:** это первый UI-элемент, завязанный на формат конкретного скилла (а не общий рендер сообщения) — если формат `brand_book.md` шага 4 изменится, регэксп в `ChatsView.tsx` надо менять синхронно; сделан промах — просто перестают появляться свотчи, деградация к обычному тексту, не поломка.
**Файлы:** `src/office/builtin_skills/brand_book.md`, `webapp/src/app/views/ChatsView.tsx`.
**Проверка:** `tsc --noEmit` + `npm run build` чистые; регэксп проверен отдельным Node-скриптом на точном примере из скилла (2 кандидата, буквы A/Б, hex, обоснование) — парсится корректно, посторонние строки (вступление/итоговый вопрос) не матчатся. Живой прогон офиса с реальным designer-вопросом не гонялся (не тратили LLM-токены на текстовую логику UI).
**Статус:** done (2026-07-15).

### #19 — Instant Learning ДО регистрации: публичный scan-эндпоинт + поле URL на лендинге
**Запрос:** из product/UX-разбора «Company Understanding как moat» — главный обязательный фикс: сканирование сайта (`company_scan.scan()`) технически уже существовало ($0, без LLM, готовые `pain_points`), но `/api/onboarding/scan` требовал авторизации (`auth_middleware`), а на самом лендинге вообще не было поля URL — вау-эффект «AI уже понимает бизнес» физически не мог случиться до формы регистрации, хотя это и есть заявленный тезис продукта.
**Реализовано:**
- `server.py` — `/api/onboarding/scan` добавлен в `_PUBLIC_API` (доступен без сессии). `company_scan.scan()` — чистая функция без побочных эффектов (не пишет в тенант), так что публичный доступ не течёт данные между тенантами.
- `server.py` (`onboarding_scan`) — rate-limit 12 запросов/мин на IP через уже существующий `_rate_limited()` (тот же механизм, что у `/api/lead`/`/auth/*`), т.к. это первый ПУБЛИЧНЫЙ эндпоинт, делающий исходящий HTTP-запрос по произвольному адресу — без лимита это готовый вектор для использования сервера как анонимного сканера/прокси.
- `src/office/company_scan.py` — **SSRF-защита** (новая, раньше не нужна была — эндпоинт был за авторизацией): `_is_private_host()` резолвит хост и блокирует loopback/private/link-local/reserved/multicast/unresolvable адреса (в т.ч. `169.254.169.254` — облачный metadata-эндпоинт). Дополнительно `follow_redirects=False` + ручной обход до 5 хопов с проверкой хоста НА КАЖДОМ хопе — иначе сайт мог бы 302-редиректнуть мимо первой проверки на внутренний адрес.
- `webapp/src/app/landing/LandingView.tsx` — новый `ScanBox` в Hero: поле URL + кнопка «Исследовать», построчный (не разом-JSON) reveal `headline`+`pain_points` с задержкой между строками — ощущение «AI изучает у меня на глазах», а не спиннер. Находки сохраняются в `sessionStorage` (`aioffice_landing_scan`) для возможного будущего подхватывания онбордингом после регистрации (сам онбординг сейчас это поле ещё не читает — отдельная задача, не входила в этот шаг).
**Осознанно НЕ сделано в этом шаге:** передача результата скана в реальный `onboarding_finish`/структурированное интервью после логина (чтобы не спрашивать то же самое дважды) — сохранено в `sessionStorage` как задел, само чтение не реализовано; распознавание стадии бизнеса (идея/запуск/рост/зрелость) и раздельные Understanding/Confidence — из того же product-разбора, но не входили в этот шаг (публичный эндпоинт + поле URL).
**Файлы:** `server.py`, `src/office/company_scan.py`, `webapp/src/app/landing/LandingView.tsx`, `tests/test_company_scan.py`.
**Тесты:** 6 новых в `tests/test_company_scan.py` (`_is_private_host` на loopback/link-local-metadata/private-ranges/public-ip/unresolvable + `scan()` на localhost без сетевого похода).
**Проверка:** живой прогон в браузере (`office-real`, порт 8020) — ввод `example.com` на лендинге → реальный `POST /api/onboarding/scan` → `200 OK` → построчный reveal находок → CTA «Сохранить это исследование». `tsc --noEmit`/`npm run build` чистые, консоль браузера без ошибок.
**Статус:** done (2026-07-15).

### #20 — Онбординг подхватывает скан с лендинга вместо повторного вопроса (закрывает первый пункт «не сделано» из #19)
**Запрос:** следующий шаг после #19 — результат Instant Learning с лендинга лежал в `sessionStorage`, но `OnboardingFlow.tsx` его не читал: после регистрации клиент видел ПУСТОЕ поле URL и его бы просканировали ВТОРОЙ раз на сервере — то самое «спрашиваем то, что уже знаем», от которого продукт должен уходить по своему же тезису.
**Реализовано:**
- `webapp/src/app/onboarding/OnboardingFlow.tsx` — на монтировании читает `sessionStorage["aioffice_landing_scan"]`, предзаполняет поле URL и показывает явную строку-подтверждение («✓ Уже посмотрел example.com — нашли 5 точек роста…») — это НЕ тихое переиспользование данных, а видимая память, что и обещано product-разбором («пользователь видит, что офис уже что-то знает»).
- `start()` передаёт готовый `scan` в `api.briefStart()` ТОЛЬКО если URL с момента лендинга не менялся (иначе это уже другой сайт, сканировать нужно заново) — сравнение `landingScan.url === url.trim()`; `sessionStorage` очищается после использования, повторный визит на `/webapp/` не покажет устаревшую подсказку.
- `webapp/src/data/api.ts` — `briefStart(input, url, scan?)`, третий параметр опционален (обратная совместимость со старым вызовом без скана не нарушена).
- `server.py` (`brief_start`) — принимает опциональный `data["scan"]`; если он есть и `ok` — использует его НАПРЯМУЮ, пропуская собственный вызов `company_scan.scan(url)` (второй сетевой поход к тому же сайту исчез); если скана нет или URL другой — прежнее поведение (сканирует сам) не тронуто.
**Осознанно НЕ сделано:** доверять `scan` из тела запроса без проверки — здесь это безопасно, т.к. это ТЕ ЖЕ ДАННЫЕ, что `/api/onboarding/scan` уже вернул тому же браузеру несколькими минутами раньше (тот же публичный эндпоинт, тот же контент, никаких новых прав не даётся); в `_is_private_host()`/rate-limit нет необходимости здесь заново, потому что реального похода на URL не происходит.
**Файлы:** `webapp/src/app/onboarding/OnboardingFlow.tsx`, `webapp/src/data/api.ts`, `server.py`.
**Проверка:** живой прогон в `DEMO_MODE` (`demo-preview`, порт 5599) — скан `example.com` на лендинге → `sessionStorage` подтверждён напрямую через devtools (`JSON.parse` содержит `url`/`result` с `headline`/`pain_points`) → дальше форма логина (GitHub/Google OAuth) физически не проходима в этом окружении без реального аккаунта, поэтому финальный шаг «поле предзаполнено + сервер не сканирует повторно» проверен чтением кода (`tsc --noEmit` чист, логика линейна: read→prefill→pass-through→clear), а не кликом до конца формы — честно фиксирую этот разрыв, а не выдаю статический анализ за живую проверку.
**Статус:** done (2026-07-15).

### #21 — Гипотеза о стадии бизнеса (LLM) на лендинге + владелец её поправляет
**Запрос:** следующий пункт из product-разбора Company Understanding после #20 — по указанию владельца сделать С LLM (не чистой эвристикой), несмотря на прямое противоречие исходному инварианту `company_scan.py` "$0, без LLM".
**Реализовано:**
- `src/office/company_scan.py` — `_stage_hypothesis()`: сначала считает `_stage_heuristic()` (сигналы CRM/аналитика/отзывы/форма → idea/launch/growth/mature, тот же принцип, что и весь остальной модуль), затем ОДИН короткий LLM-вызов (`glm-4.5-flash`, `max_tokens=200`) пытается уточнить формулировку; при любой ошибке/невалидном JSON/таймауте — используется эвристика как safety-фолбэк, скан никогда не падает и не виснет из-за LLM.
- **Живой замер вскрыл реальную проблему, не гипотетическую:** обычный вызов без спец-таймаута занял **156.9 секунды** (apinet/glm-4.5-flash в этой сессии) — абсолютно неприемлемо для лендинга перед регистрацией. Добавлен `_STAGE_LLM_TIMEOUT = 8.0` (`asyncio.wait_for`), независимый от общего `llm.CALL_TIMEOUT=180` (тот рассчитан на настоящие рабочие задачи агентов, не на страницу, где посетитель ждёт секунды). При таймауте — мгновенный откат на эвристику, скан НЕ висит.
- `server.py` — `_PUBLIC_API`/rate-limit не менялись: это тот же вызов, что уже был лимитирован в issue #19; отдельно отмечено (докстринг `_stage_hypothesis`), что теперь КАЖДЫЙ анонимный визит с непустым URL стоит реальных денег с общего баланса платформы (тенант "default", не тенант конкретного клиента) — осознанный компромисс ради вау-эффекта, ограниченный тем же rate-limit 12/мин/IP и дешёвой моделью.
- `webapp/src/app/landing/LandingView.tsx` — карточка гипотезы под находками («Похоже, вы в активном росте — …») + 4 кнопки-корректировки (по числу стадий); клик по «не та стадия» правит `result.stage` на клиенте И в `sessionStorage` (та же память, что уже нёс #20 в онбординг) — это ИМЕННО гипотеза с явным исправлением, не тихий вердикт.
**Осознанно НЕ сделано:** передача исправленной стадии в реальный бриф/CEO-контекст после регистрации — сохранена в `sessionStorage` как задел (следующий логичный шаг был бы прочитать её в `OnboardingFlow.tsx`/`build_brief`, как уже сделано для самого скана в #20), но не входило в этот шаг.
**Файлы:** `src/office/company_scan.py`, `webapp/src/app/landing/LandingView.tsx`, `tests/test_company_scan.py`.
**Тесты:** 8 новых в `tests/test_company_scan.py` — 5 на `_stage_heuristic` (все 4 стадии + пустой `detected`), 3 на `_stage_hypothesis` (валидный LLM-JSON используется / фолбэк на ошибке / **фолбэк на медленном ответе через мокнутый `_STAGE_LLM_TIMEOUT=0.05`** — тест, который прямо кодирует найденный прод-баг).
**Проверка:** живой прогон на `office-real` (порт 8020, реальный `LLM_API_KEY`) — ДО фикса таймаута: `stripe.com` завис на 45+ секунд без ответа сервера (баг найден живым тестом, не код-ревью). ПОСЛЕ фикса: тот же ввод → `200 OK` за секунды → карточка «Похоже, вы на стадии запуска — …» с 4 кнопками → клик «Активно растём» → `sessionStorage` подтверждён через devtools (`stage.confirmed: true, key: "growth"`). Заодно поправлена дублирующаяся формулировка эвристики launch/idea («похоже» повторялось дважды в одном предложении) — найдено при этой же живой проверке.
**Статус:** done (2026-07-15).

### #22 — Стадия бизнеса доходит до реального брифа CEO (закрывает «осознанно не сделано» из #21)
**Запрос:** следующий шаг после #21 — подтверждённая/исправленная владельцем стадия бизнеса лежала только в `sessionStorage` фронтенда; сам CEO/воркеры её никогда не видели, хотя #20 уже показал рабочий паттерн для скана целиком (carry-over вместо повторного вопроса), и стадия должна пройти тот же путь.
**Реализовано:**
- `server.py` (`brief_start`) — после сборки брифа, если `scan_result.get("stage")` есть (в т.ч. поправленная владельцем на лендинге версия — она уже часть объекта, который шлёт `OnboardingFlow.tsx`), кладёт её в `brief_data["business_stage"]` отдельным структурированным полем.
- `src/agents/onboarding.py` (`build_brief_structured`) — то же поле добавлено и в детерминированный путь сборки брифа (структурированное интервью), для консистентности источника правды.
- `src/office/prompt_builder.py` (`brief_block`) — **единственный сериализатор брифа в промпт** (инвариант CLAUDE.md §4) получил новую строку: «Вероятная стадия бизнеса (предположение, не факт) / (подтверждено владельцем): …». Явная пометка confirmed/не confirmed обязательна — агент не должен принять непроверенную эвристику/LLM-гипотезу за бесспорный факт о бизнесе клиента (тот же принцип, что уже действует для `goal` vs `niche` — не путать источники разной надёжности).
**Осознанно НЕ сделано:** ветвление логики CEO/plan.py по стадии бизнеса (например, другой набор milestones для "idea" против "mature") — стадия сейчас ТОЛЬКО информационная строка в промпте, не структурный вход в планирование; если завтра выяснится, что этого мало, следующий шаг — научить `orchestrator.plan_milestones` читать `brief.get()["business_stage"]["key"]` явно.
**Файлы:** `server.py`, `src/agents/onboarding.py`, `src/office/prompt_builder.py`, `tests/test_no_stack_lockin.py`.
**Тесты:** 2 новых в `tests/test_no_stack_lockin.py` (business_stage сериализуется с меткой "предположение" для неподтверждённой гипотезы и "подтверждено владельцем" для исправленной/принятой).
**Статус:** done (2026-07-15).

### #23 — Ветвление CEO по стадии бизнеса в детерминированном плане (закрывает «осознанно не сделано» из #22)
**Запрос:** явный выбор владельца из предложенных в #22 направлений — научить план реагировать на `business_stage`, а не только показывать его в промпте.
**Где реально ветвление, а где уже было:** LLM-путь (`orchestrator.plan_tasks`) уже видел `business_stage` через `prompt_builder.brief_block()` (issue #22) — модель МОЖЕТ сама учесть его при генерации графа задач, но это не гарантия. Единственное место, где ветвления не было вообще — **детерминированный фолбэк** (`planning_engine.fallback_plan`), который используется, когда LLM-генерация недоступна (слабая модель/429/битый JSON) и ДОЛЖЕН быть предсказуем сам по себе (CLAUDE.md §4 «Plan-driven всегда»).
**Реализовано:**
- `src/office/planning_engine.py` (`fallback_plan`) — новый параметр `business_stage: str = ""`. Единственное новое ветвление: явная просьба сайта/лендинга (`wants_site`) + стадия `growth`/`mature` → задача `analyst` (проанализировать УЖЕ существующий сайт/CRM, данные автоскана уже есть в брифе — не сканировать заново, найти 2-3 конкретные точки роста) → задача `developer` (улучшить ТОЧЕЧНО, не пересобирать с нуля). Обоснование: growth/mature стадия эвристически определяется ПО НАЛИЧИЮ CRM/аналитики НА сайте клиента (`company_scan._stage_heuristic`) — значит сайт уже реально существует и работает, слепая пересборка с нуля игнорировала бы это.
- `launch`/`idea`/пустая стадия (нет гипотезы) — поведение НЕ изменилось: сайта скорее всего ещё нет или он минимален, пересборка с нуля остаётся правильным дефолтом.
- `wants_bot`-путь и generic-путь ("развивать бизнес" без явного продукта) — `business_stage` намеренно игнорируется: growth-клиент, попросивший бота, не должен получить план аудита сайта вместо бота.
- `src/office/loop.py` — единственный call site: читает `brief.get()["business_stage"]["key"]` и передаёт в `fallback_plan`, не дублируя логику чтения брифа в другом месте.
**Осознанно НЕ сделано:** ветвление `orchestrator.plan_milestones`/`decide_company` (структура этапов/отделов) по стадии — стадия сейчас влияет только на КОНКРЕТНЫЕ задачи фолбэк-плана, не на верхнеуровневые этапы/оргструктуру; расширять дальше — только под конкретный найденный кейс, не «на всякий случай» (тот же принцип, что и в #15 про denylist).
**Файлы:** `src/office/planning_engine.py`, `src/office/loop.py`, `tests/test_planning_engine.py`.
**Тесты:** 4 новых в `tests/test_planning_engine.py` (growth-аудит вместо пересборки, mature — та же ветка, bot-путь стадию игнорирует, launch/idea/пустая стадия — поведение не изменилось).
**Статус:** done (2026-07-15).

### #24 — Confidence отдельно от Understanding score + разбивка по доменам в UI
**Запрос:** из исходного product-разбора Company Understanding — «не Understanding, а два числа: Understanding (сколько известно) и Confidence (насколько уверены)». Дополнительно найдено: `understanding.py` уже считал разбивку по 5 доменам (`domains`), но фронтенд её НИГДЕ не показывал — посчитано, но невидимо.
**Реализовано:**
- `src/office/understanding.py` — новая `_confidence()`: отдельная от `score` метрика. Score = СКОЛЬКО данных есть (широта: заполнен бриф, написаны документы). Confidence = НАСКОЛЬКО им можно доверять — самоотчёт клиента весит меньше, чем то, что офис ПРОВЕРИЛ САМ (автоскан сайта: +25, реальные подключённые интеграции: до +40, CRM-виджет на сайте клиента: +10, внешнее рыночное исследование: +15). Возвращает `confidence` (0-100) + `confidence_reasons` (список факторов, как во free-form self-report на лендинге, issue #21).
- **Побочный найденный баг:** и в старом коде, и в первой версии Confidence — Tool Router capability-заглушки (`src/integrations/{crm,ads,payments,deploy,invoicing}.py`, `cred_fields=[]`, как `website`) всегда "connected" независимо от клиента. `crm_connected` (использовался в разбивке доменов ДО этого issue) считал такую заглушку реальным подключением CRM — **любой** тенант получал +60 к домену "sales" без единого реального подключения. Исправлено фильтром по `cred_fields`/`oauth_url` — не по названию (устойчиво к новым заглушкам).
- `webapp/src/data/api.ts` — `understanding()` фолбэк расширен (`domains`, `confidence`, `confidence_reasons`).
- `webapp/src/app/App.tsx` — попап «Статус офиса»: (1) разбивка по доменам как полоски-прогресс-бары (`DOMAIN_LABELS`) — ЖИВАЯ, а не мёртвые данные; (2) отдельный блок «🎯 Уверенность в выводах — N%» с перечнем факторов, явно НЕ смешан с «Данные о бизнесе» (обе подписи взаимно ссылаются друг на друга, чтобы не путали, как уже сделано для «Здоровья работы»).
**Файлы:** `src/office/understanding.py`, `webapp/src/data/api.ts`, `webapp/src/app/App.tsx`, `tests/test_company_scan.py`.
**Тесты:** 4 новых в `tests/test_company_scan.py` (confidence присутствует и в границах; автоскан даёт больше confidence, чем голый self-report; пустой бриф держит confidence низким; домен "sales" больше не раздут CRM-заглушкой).
**Проверка:** живой прогон в `DEMO_MODE` (порт 5599) — `/api/brief/start` с реальным (дешёвым) LLM-вызовом → главный экран → клик по бейджу статуса → попап показывает «Бизнес 70% / Маркетинг 20% / Продажи 10% / Финансы 10% / Команда 5%» и «Уверенность в выводах — 10%» с причиной «есть описание бизнеса (со слов клиента)» — домен "sales" подтверждён на 10%, не на раздутых 60%. `tsc --noEmit`/`npm run build` чистые, консоль без ошибок.
**Статус:** done (2026-07-15).

### #25 — 4-я категория пользователей (Enterprise/BOS): интеграции подбираются по стадии, не только по словам
**Запрос:** из исходного product-разбора — 4 типа клиента (идея/малый бизнес/растущий бизнес/крупная компания), причём для крупной «AI подключается к существующей инфраструктуре... работает как Business Operating System».
**Важное архитектурное решение ДО реализации:** в коде уже есть 3-сценарийный структурированный онбординг (`onboarding.MODES` — business/launch/idea, `/api/onboarding/modes`+`/finish`) — но он НЕ подключён к текущему фронтенду (`OnboardingFlow.tsx` использует только свободный флоу `/api/brief/start`, `onboardingModes()` в `api.ts` нигде не вызывается). Добавление 4-го режима туда не дало бы пользователю никакого эффекта — мёртвый код поверх мёртвого кода. **По явному выбору владельца** — реализовано в РЕАЛЬНОМ используемом флоу.
**Реализовано:**
- `src/integrations/registry.py` (`suggested_for`) — новый параметр `business_stage: str = ""`. Для `growth`/`mature` (эвристика/LLM-гипотеза по автоскану сайта, issue #19/#21) интеграции из `_ENTERPRISE_INTEGRATIONS` (`crm`, `crm_bitrix24`, `erp_1c`, `google_sheets`) получают +1 к скору совпадения ДАЖЕ без буквального упоминания в тексте брифа — крупный клиент обычно пишет «автоматизировать процессы», а не называет конкретные системы, и старый чисто keyword-based подбор его вообще не находил.
- `server.py` (`/api/onboarding/suggested-integrations`) — читает `brief.get()["business_stage"]["key"]` и передаёт в `suggested_for` (тот же паттерн чтения стадии, что уже в `loop.py`/#23).
**Осознанно НЕ сделано:** отдельный UI-сценарий "Enterprise" с другим онбордингом — интеграции просто ПОЯВЛЯЮТСЯ раньше и точнее в уже существующем экране `IntegrationsScreen` (`OnboardingFlow.tsx`), без нового UI; отдельная ветка стала бы решением ради решения, а не ради результата, который реально увидит пользователь.
**Файлы:** `src/integrations/registry.py`, `server.py`, `tests/test_onboarding_result.py`.
**Тесты:** 3 новых в `tests/test_onboarding_result.py` (без стадии — прежнее поведение; growth-стадия добавляет enterprise-интеграции без ключевых слов; launch/idea/пустая стадия буст не применяют).
**Проверка:** живой прогон в `DEMO_MODE` — бриф «хочу автоматизировать процессы» без стадии → `suggested-integrations` вернул только `["telegram"]` (ложное срабатывание по слову «процесс»/тексту брифа, не относится к тесту); после патча `business_stage.key="growth"` (с перезапуском сервера — `saas/context.py` кеширует бриф в памяти процесса, отдельный CLI-патч файла не подхватывается без рестарта, что и обнаружилось при проверке) тот же запрос вернул `["telegram", "google_sheets", "crm"]` — буст подтверждён вживую, не только юнит-тестом.
**Статус:** done (2026-07-15).
