openai>=1.40.0
ddgs>=6.0.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
sse-starlette>=2.1.0
httpx>=0.27.0
PyJWT>=2.8.0
cryptography>=42.0.0
python-multipart>=0.0.9   # приём заявок из нативных HTML-форм (multipart/urlencoded)
telethon>=1.44.0           # личный Telegram-аккаунт (MTProto) — писать в ЛС лидам,
                           # в отличие от Bot API, которое не может начать диалог первым

# Опционально: «зрячая» проверка сайта критиком (рендер в headless-браузере).
# Без него критик работает, но пропускает визуальные проверки (overflow/консоль).
# Включить: pip install playwright && playwright install chromium
# playwright>=1.44.0

pytest>=8.0.0   # tests/ — раньше единственной проверкой был py_compile/tsc (см. CLAUDE.md)
