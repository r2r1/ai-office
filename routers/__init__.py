"""
HTTP-роутеры server.py по доменам (docs/technical-due-diligence-2026-07-17.md §3.2.1,
PR-5): server.py был 2710 строк на 164 маршрута в одном файле — невозможно
параллельно ревьюить изменения в разных доменах без конфликтов мержа, поиск
роута требовал grep вместо навигации по структуре.

Каждый файл здесь — fastapi.APIRouter, подключаемый в server.py через
app.include_router(...). Домен переезжает МЕХАНИЧЕСКИ (тот же код, то же
поведение) — сначала auth (самый изолированный), остальные домены следуют
по одному за раз (см. план в docs/technical-due-diligence-2026-07-17.md §12).

Общие для нескольких роутеров помощники (_client_ip, current_user,
_rate_limited) — в routers/shared.py, не дублируются по файлам.
"""
